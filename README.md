# Tristan's Daily

## 本次改动
1. 上传区新增：
   - 删除上传文件（带“是否删除该上传文件？”确认）
   - 保存上传的 md 到文档（可选择保存为“随想/工作日志”，可选择是否精选）
2. Markdown 识别增强（更稳的解析链路）：
   - 切换为 `markdown-it` 解析器
   - 支持 GFM 常见语法、自动链接、换行、排版优化
   - 保留数学公式 `$$..$$` `\[..\]` `$..$` `\(..\)`
   - 支持代码块高亮（highlight.js）
   - 支持高亮 `==text==` 与下划线 `++text++`
3. 界面美术优化：上传区卡片化操作按钮、更一致的信息密度与层次。

## 存储
- 本地存储：`localStorage`
- 键：`tristan_entries_v5`、`tristan_uploads_v5`
