# Tristan's Daily

## 本次改动
1. 导航里的“分类”改为“文档”。
2. 文档列表每条 md 文档新增两个按钮：`浏览`（只读页）和 `编辑`（进入撰写页）。
3. 精选列表同样增加 `浏览` 与 `编辑`。
4. 每次从撰写页切换到其他功能页，再回来会自动开启新的空白撰写文档（不自动保留上一个编辑态）。
5. Markdown 渲染增加公式支持：`$$...$$`、`\(...\)`、`\[...\]`。

## 存储
- 本地存储：`localStorage`
- 键：`tristan_entries_v3`、`tristan_uploads_v3`
