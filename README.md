# Tristan's Daily

## 本次改动
1. PDF 上传后支持“保存到文档”按钮：可选择保存为“随想/工作日志”，并可选择是否精选。
2. PDF 上传项继续保留“浏览 + 删除”，浏览为页面内 iframe。
3. 美术继续升级：
   - 侧边栏改为淡黄色古纸感（不再偏灰）
   - 背景加入半嵌入沙漠图层（隐约可见）
   - 卡片、按钮、阴影、层次统一
4. 英文字体默认改为 `Times New Roman`；新增字体切换按钮，可切换到 `Georgia`。

## 存储
- 本地存储：`localStorage`
- 键：`tristan_entries_v5`、`tristan_uploads_v5`
